# SPDX-License-Identifier: GPL-2.0
#
# Kbuild for AcreetionOS Linux Kernel
# Copyright (c) ArttulOS Project
#
# This file is part of the AcreetionOS kernel build system.
# It defines the top-level build targets and subdirectories.

# Top-level Kbuild file

# Generic headers
generic-y += export.h

# Architecture-specific build targets
obj-y := init/
obj-y += kernel/
obj-y += mm/
obj-y += fs/
obj-y += ipc/
obj-y += net/
obj-y += drivers/

# Scripts for build system
obj-y += scripts/
